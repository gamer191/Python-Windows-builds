name: Build Python
on: push
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Python
        uses: actions/checkout@v6
        with:
          repository: python/cpython
          ref: "3.10"
      - run: cmd /c PCbuild\build.bat -e -p x64
      # - run: cmd /c PCbuild\rt.bat
      - uses: actions/upload-artifact@v6
        with:
          path: PCbuild
